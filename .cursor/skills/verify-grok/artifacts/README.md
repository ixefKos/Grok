# Verification evidence

Proof artifacts from `control-grok` runs live here as `artifacts/<run-id>/<feature>/`.

Cleanup removes disposable `/tmp/grok-verify-*` state only; these files are retained on purpose.
